

// ES6 APROCH

class Circle {

  constructor(value, randomX, randomY) {
    this.value = value;
    this.randomX = randomX;
    this.randomY = randomY;

  }

}

let circle = new Circle(1, 100, 100);

document.body.appendChild(circle)
